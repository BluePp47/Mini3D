using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testutf : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //한끌깨짐
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
